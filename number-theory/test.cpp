#include "nt.hpp"

using namespace std;

main(){
  ios::sync_with_stdio(false);
  cin.tie(0);
  
  cout << GCD(72,36) << endl;
  cout << LCM(3,4) << endl;
  cout << CRT({3, 5, 7}, {2, 3, 2}) << endl;
}
